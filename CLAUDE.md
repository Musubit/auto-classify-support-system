## Agent skills

### Issue tracker

本地 markdown（`.scratch/<issue-slug>/issue.md`）。见 `docs/agents/issue-tracker.md`。

### Triage labels

使用标准五状态标签，名称未覆盖。见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文仓库（一个 `CONTEXT.md` + `docs/adr/`）。见 `docs/agents/domain.md`。
